import 'package:flutter/material.dart';

class MasterSearchCard extends StatelessWidget {
  const MasterSearchCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
